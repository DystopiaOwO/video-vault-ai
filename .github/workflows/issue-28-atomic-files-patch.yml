name: Apply Issue 28 atomic file patch

on:
  pull_request:
    branches:
      - main
    types:
      - synchronize
      - reopened

permissions:
  contents: write

jobs:
  patch:
    if: github.event.pull_request.head.ref == 'codex/issue-28-perception-runs'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/issue-28-perception-runs
      - name: Apply early failure and atomic file patch
        run: python .github/scripts/apply_issue28_atomic_files.py
      - name: Commit patch and remove temporary files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/scripts/apply_issue28_atomic_files.py
          git rm .github/workflows/issue-28-atomic-files-patch.yml
          git add src/video_vault/perception_runs.py src/video_vault/planner.py src/video_vault/project.py
          git commit -m "fix: persist early failures and atomically replace plans"
          git push
