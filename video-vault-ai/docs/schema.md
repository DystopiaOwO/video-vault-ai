# Schema

SQLite tables:

- `videos`: original/current path, metadata, category, proxy, status.
- `frames`: extracted frame paths and later vision scores.
- `segments`: suggested editing ranges.
- `analysis_runs`: provider/model/status/raw output path.
