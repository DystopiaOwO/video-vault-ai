import type { RenderSettings } from "../../api";
export function RenderSettingsPanel({ settings, onChange, disabled }: { settings: RenderSettings; onChange: (value: RenderSettings) => void; disabled?: boolean }) {
  const set = (patch: Partial<RenderSettings>) => onChange({ ...settings, ...patch });
  return <div className="settings-panel"><label>預覽 Profile<select disabled={disabled} value={settings.previewProfile} onChange={(e) => set({ previewProfile: e.target.value })}><option>preview_1080p30</option><option>preview_1080p60</option></select></label><label>正式 Profile<select disabled={disabled} value={settings.finalProfile} onChange={(e) => set({ finalProfile: e.target.value })}><option>final_1080p30</option><option>final_1080p60</option><option>final_2160p30</option></select></label><label>調色模式<select disabled={disabled} value={settings.colorMode} onChange={(e) => set({ colorMode: e.target.value })}><option value="none">不調色</option><option value="dji_lut">DJI LUT</option><option value="safe_restore">保守修正</option></select></label><label>BGM 音量<input disabled={disabled} type="number" min="0" max="1" step=".05" value={settings.bgmVolume} onChange={(e) => set({ bgmVolume: Number(e.target.value) })} /></label><label>音訊交疊（秒）<input disabled={disabled} type="number" min="0" max="2" step=".01" value={settings.audioCrossfade} onChange={(e) => set({ audioCrossfade: Number(e.target.value) })} /></label><label>編碼器<select disabled={disabled} value={settings.encoder} onChange={(e) => set({ encoder: e.target.value })}><option>auto</option><option>h264_nvenc</option><option>libx264</option></select></label><label className="check"><input disabled={disabled} type="checkbox" checked={settings.overlayEnabled} onChange={(e) => set({ overlayEnabled: e.target.checked })} /> 套用字卡</label></div>;
}
