import type { Segment } from "../../api";
export function JoinPreviewPanel({ segments }: { segments: Segment[] }) { const selected = segments.filter((item) => item.include); const duration = selected.reduce((sum, item) => sum + Math.max(0, item.end_seconds - item.start_seconds) / Math.max(.25, item.speed), 0); return <section className="panel"><h2>接合預覽</h2><p>{selected.length} 個片段 · 預估長度 {duration.toFixed(3)} 秒</p><ol>{selected.map((item) => <li key={item.segment_id}>{item.title} <span className="muted">{item.start_seconds.toFixed(3)} ~ {item.end_seconds.toFixed(3)}</span></li>)}</ol></section>; }
